scalar Int64

schema {
    query: Query
    mutation: Mutation
}
type Query {
    user(id: Int!): User
    users: [User]
}

type Mutation {
    addUser(firstname: String!, lastname: String!, roles:[String]!): User
}
